# Broadstreet API Configuration
# Use 'demo-token-for-development' for development mode with mock data
# For production, use your actual Broadstreet API token
BROADSTREET_API_TOKEN=demo-token-for-development
BROADSTREET_API_BASE_URL=https://api.broadstreetads.com/api/1

# Database Configuration (for production)
MONGODB_URI=mongodb://localhost:27017/broadstreet-campaigns

# Next.js Configuration
NEXT_PUBLIC_APP_NAME=Broadstreet Publishing Dashboard

# CORS Configuration
# Comma-separated list of allowed origins for API requests
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004,http://localhost:3005,http://local.ft.tc,https://broad.travelm.de

# Production Configuration Notes:
# - API Base URL must be https://api.broadstreetads.com/api/1 (not /v1)
# - API token should be your actual Broadstreet API access token
# - The app will automatically detect demo vs production mode based on token value
# - Add or remove origins from CORS_ALLOWED_ORIGINS as needed
